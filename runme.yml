---

# all the things

- name: Configure my home lab systems
  hosts: all

  pre_tasks:

    - name: Verify if distro is supported
      ansible.builtin.fail:
        msg: "This playbook is not compatible with the host's distribution."
      when: >
        (ansible_facts["distribution"] != "Rocky" or ansible_facts["distribution_major_version"] != "10") and
        (ansible_facts["distribution"] != "Ubuntu" or ansible_facts["distribution_major_version"] != "24")

  roles:
    - role: common
      tags:
        - install
        - upgrade

    - role: reppocs.minecraft
      when: "'minecraft' in group_names"
      tags:
        - minecraft
