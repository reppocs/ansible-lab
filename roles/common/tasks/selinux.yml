---

# disable selinux

- name: disable selinux
  become: true
  block:

    - name: disable selinux
      ansible.posix.selinux:
        state: disabled

    - name: reboot host
      reboot:

# end of disable selinux block

# EOF
